This command, which takes 2 arguments, allows to move a file from one directory to another. 
File origin (if not in current directory) and destination must be specified.
The mv command can be used to rename a file by simply moving a file within the same directory while changing its name.
